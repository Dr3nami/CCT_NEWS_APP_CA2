import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'CCT_NEWS_CA2',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
